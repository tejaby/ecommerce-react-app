// components
import { Layout } from "../../components/layouts/Layout";
import { CartContent } from "../../components/cart/CartContent";

export const Cart = () => {
  return (
    <Layout>
      <CartContent />
    </Layout>
  );
};
